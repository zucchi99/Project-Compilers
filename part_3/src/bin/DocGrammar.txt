The Language grammar
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of grammar==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in grammar are the following:
  | ``and`` | ``array`` | ``begin`` | ``boolean``
  | ``char`` | ``const`` | ``div`` | ``else``
  | ``end`` | ``false`` | ``function`` | ``if``
  | ``integer`` | ``mod`` | ``not`` | ``of``
  | ``or`` | ``procedure`` | ``program`` | ``readChar``
  | ``readInt`` | ``readReal`` | ``readString`` | ``real``
  | ``string`` | ``then`` | ``true`` | ``var``
  | ``writeChar`` | ``writeInt`` | ``writeReal`` | ``writeString``

The symbols used in grammar are the following:
  | ; | . | = | :
  | ( | ) | , | :=
  | > | < | >= | <=
  | + | - | * | /
  | ** | [ | ] | ^

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with { and }, (* and *).

==The syntactic structure of grammar==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //BeginKW// | -> | ``begin``
  | //EndKW// | -> | ``end``
  | //ProgramKW// | -> | ``program``
  | //FunctionKW// | -> | ``function``
  | //ProcedureKW// | -> | ``procedure``
  | //VarKW// | -> | ``var``
  | //ConstKW// | -> | ``const``
  | //IfKW// | -> | ``if``
  | //ThenKW// | -> | ``then``
  | //ElseKW// | -> | ``else``
  | //IntKW// | -> | ``integer``
  | //RealKW// | -> | ``real``
  | //CharKW// | -> | ``char``
  | //BoolKW// | -> | ``boolean``
  | //StringKW// | -> | ``string``
  | //ArrayKW// | -> | ``array``
  | //Program// | -> | //ProgramKW// //Ident// ``;`` //InnerBlockWithDecl// ``.``
  | //InnerBlockWithDecl// | -> | //[Declaration]// //InnerBlockExec//
  | //[Declaration]// | -> | **eps**
  |  |  **|**  | //Declaration// //[Declaration]//
  | //InnerBlockExec// | -> | //BeginKW// //NonMandatoryTerminator// //[Statement]// //EndKW//
  | //[Statement]// | -> | **eps**
  |  |  **|**  | //Statement//
  |  |  **|**  | //Statement// ``;`` //[Statement]//
  | //NonMandatoryTerminator// | -> | **eps**
  |  |  **|**  | ``;``
  | //Declaration// | -> | //CostantsBlock//
  |  |  **|**  | //VariablesBlock//
  |  |  **|**  | //FunctionDecl//
  |  |  **|**  | //ProcedureDecl//
  | //CostantsBlock// | -> | //ConstKW// //[ConstantDecl]//
  | //[ConstantDecl]// | -> | //ConstantDecl// ``;``
  |  |  **|**  | //ConstantDecl// ``;`` //[ConstantDecl]//
  | //ConstantDecl// | -> | //Ident// ``=`` //RightExp//
  | //VariablesBlock// | -> | //VarKW// //[VariableDeclBlock]//
  | //[VariableDeclBlock]// | -> | //VariableDeclBlock// ``;``
  |  |  **|**  | //VariableDeclBlock// ``;`` //[VariableDeclBlock]//
  | //[VariableDeclFunc]// | -> | **eps**
  |  |  **|**  | //VariableDeclFunc//
  |  |  **|**  | //VariableDeclFunc// ``;`` //[VariableDeclFunc]//
  | //VariableDeclBlock// | -> | //[VariableName]// ``:`` //Type//
  | //DeclarationFunc// | -> | **eps**
  |  |  **|**  | ``(`` //[VariableDeclFunc]// ``)``
  | //VariableDeclFunc// | -> | //[VariableName]// ``:`` //Type//
  | //[VariableName]// | -> | //VariableName//
  |  |  **|**  | //VariableName// ``,`` //[VariableName]//
  | //VariableName// | -> | //Ident//
  | //FunctionDecl// | -> | //FunctionKW// //Ident// //DeclarationFunc// ``:`` //Type// ``;`` //InnerBlockWithDecl// ``;``
  | //ProcedureDecl// | -> | //ProcedureKW// //Ident// //DeclarationFunc// ``;`` //InnerBlockWithDecl// ``;``
  | //FunctionCall// | -> | //Ident// ``(`` //[RightExp]// ``)``
  | //ProcedureCall// | -> | //Ident// ``(`` //[RightExp]// ``)``
  | //[RightExp]// | -> | **eps**
  |  |  **|**  | //RightExp//
  |  |  **|**  | //RightExp// ``,`` //[RightExp]//
  | //Statement// | -> | //InnerBlockExec//
  |  |  **|**  | //If//
  |  |  **|**  | //Assign//
  |  |  **|**  | //FunctionCall//
  |  |  **|**  | //ProcedureCall//
  |  |  **|**  | //WritePrimitive//
  |  |  **|**  | //ReadPrimitive//
  | //Assign// | -> | //LeftExp// ``:=`` //RightExp//
  | //RightExp// | -> | //RightExp1//
  |  |  **|**  | //RightExp// ``or`` //RightExp1//
  | //RightExp1// | -> | //RightExp2//
  |  |  **|**  | //RightExp1// ``and`` //RightExp2//
  | //RightExp2// | -> | //RightExp3//
  |  |  **|**  | //RightExp2// ``>`` //RightExp3//
  |  |  **|**  | //RightExp2// ``<`` //RightExp3//
  |  |  **|**  | //RightExp2// ``>=`` //RightExp3//
  |  |  **|**  | //RightExp2// ``<=`` //RightExp3//
  |  |  **|**  | //RightExp2// ``=`` //RightExp3//
  | //RightExp3// | -> | //RightExp4//
  |  |  **|**  | //RightExp3// ``+`` //RightExp4//
  |  |  **|**  | //RightExp3// ``-`` //RightExp4//
  | //RightExp4// | -> | //RightExp5//
  |  |  **|**  | //RightExp4// ``*`` //RightExp5//
  |  |  **|**  | //RightExp4// ``/`` //RightExp5//
  |  |  **|**  | //RightExp4// ``mod`` //RightExp5//
  |  |  **|**  | //RightExp4// ``div`` //RightExp5//
  | //RightExp5// | -> | //RightExp6//
  |  |  **|**  | //RightExp5// ``**`` //RightExp6//
  | //RightExp6// | -> | //RightExp7//
  |  |  **|**  | ``not`` //RightExp7//
  |  |  **|**  | ``-`` //RightExp7//
  |  |  **|**  | ``+`` //RightExp7//
  | //RightExp7// | -> | ``(`` //RightExp// ``)``
  |  |  **|**  | //Ident//
  |  |  **|**  | //Integer//
  |  |  **|**  | //Double//
  |  |  **|**  | //Boolean//
  |  |  **|**  | //Char//
  |  |  **|**  | //String//
  |  |  **|**  | //FunctionCall//
  | //LeftExp// | -> | //Ident//
  | //If// | -> | //IfKW// //RightExp// //ThenKW// //Statement// //Else//
  | //Else// | -> | **eps**
  |  |  **|**  | //ElseKW// //Statement//
  | //Type// | -> | //BaseType//
  |  |  **|**  | //CompositeType//
  | //BaseType// | -> | //IntKW//
  |  |  **|**  | //RealKW//
  |  |  **|**  | //CharKW//
  |  |  **|**  | //BoolKW//
  |  |  **|**  | //StringKW//
  | //Boolean// | -> | ``true``
  |  |  **|**  | ``false``
  | //CompositeType// | -> | //ArrayKW// ``[`` //Integer// ``]`` ``of`` //BaseType//
  |  |  **|**  | ``^`` //Type//
  | //WritePrimitive// | -> | ``writeInt`` ``(`` //RightExp// ``)``
  |  |  **|**  | ``writeReal`` ``(`` //RightExp// ``)``
  |  |  **|**  | ``writeChar`` ``(`` //RightExp// ``)``
  |  |  **|**  | ``writeString`` ``(`` //RightExp// ``)``
  | //ReadPrimitive// | -> | ``readInt`` ``(`` //LeftExp// ``)``
  |  |  **|**  | ``readReal`` ``(`` //LeftExp// ``)``
  |  |  **|**  | ``readChar`` ``(`` //LeftExp// ``)``
  |  |  **|**  | ``readString`` ``(`` //LeftExp// ``)``


